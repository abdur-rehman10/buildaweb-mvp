name: Deploy Staging

on:
  push:
    branches:
      - develop

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging deployment will be configured after ECS staging setup."
